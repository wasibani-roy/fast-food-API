language: python
python:
  - "3.6"
# command to install dependencies
install: pip install -r requirements.txt
# command to run tests
script: py.test test_orders.py --doctest-modules --pep8 coveralls -v --cov coveralls --cov-report term-missing
branches:only: api
after_success:
  - coveralls
